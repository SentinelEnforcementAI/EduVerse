# Dependencies
node_modules/

# Environment — secrets live in .env only, never committed
.env
.env.*
!.env.example

# Next.js
.next/
out/

# Build output
dist/
*.tsbuildinfo

# Test output
coverage/

# Misc
.DS_Store
*.log
**/prisma/seed-manifest.json
dump.rdb

# Demo tour: local capture context (mints session cookies) and on-demand
# embedded gallery output. The screens/ and thin gallery.html ARE committed.
tooling/screenshots/.context.json
docs/demo/tour/*.embedded.html
