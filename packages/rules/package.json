{
  "name": "@sentinel/rules",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "dotenv -e ../../.env -- vitest run",
    "run-rules": "dotenv -e ../../.env -- tsx src/cli.ts"
  },
  "dependencies": {
    "@sentinel/db": "workspace:*"
  },
  "devDependencies": {
    "@sentinel/sync": "workspace:*",
    "@types/node": "^22.20.1",
    "dotenv-cli": "^11.0.0",
    "tsx": "^4.23.1",
    "typescript": "^5",
    "vitest": "^3.2.7"
  }
}
